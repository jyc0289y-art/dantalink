# 품질검사 #3 — 콘솔 연계 + 클라이언트 매칭 엔진 보강 검증

- **시각**: 2026-05-20 15:02:58 (KST)
- **대상 작업**: commit `1ea0a2d` (Stage 01 ↔ Stage 02 콘솔 연계 + 클라이언트 매칭 엔진)
- **검사자**: Claude Code 자동 검증 스크립트
- **결과**: ✅ **PASS** (자동 검증 4/4, E2E 1건 사용자 확인 대기)
- **메모**: 본 QC는 사용자 지적(2026-05-20 14:55) 이후 **사후 보강 검증**으로, 표준 워크플로우상으로는 작업 직후 수행됐어야 함

---

## 1. 자동 검증 결과 (4/4 PASS)

| # | 항목 | 기대 | 실측 | 판정 |
|---|------|------|------|------|
| 1 | pytest 회귀 (Python 매칭 모듈) | 15 passed | 15 passed in 0.16s | ✅ |
| 2 | Pages URL 검증 | 6개 모두 200 OK | 6개 모두 200 OK | ✅ |
| 3 | JS↔Python 매칭 결과 일치 | Top 10 동일 | **Top 10 완전 동일** + 핵심 종목 3개 점수 정확 일치 | ✅ |
| 4 | KRX DB CSV↔JSON 일관성 | 2,878 종목 모두 일치 | **2,878 종목 4개 컬럼 완전 일치** | ✅ |

### 1.1 항목 3 상세 — JS↔Python Cross-Check
```
환경: Node v24.14.0
입력: events_demo.json (3건)
필터: minMarcap=100, maxMarcap=5000, maxPerToken=5, maxPerKeyword=5

JS 결과 (matcher_engine.js):  Top 117 candidates → Top 10
PY 결과 (matches_demo.json):  Top 30 candidates  → Top 10

순위 1~10 비교:
  순위  종목                       JS max/total | PY max/total      판정
  ──────────────────────────────────────────────────────────────
   1.  아이진(185490)              0.55/0.55    | 0.55/0.55       ✓
   2.  중앙백신(072020)             0.50/0.50    | 0.50/0.50       ✓
   3.  차백신연구소(261780)         0.50/0.50    | 0.50/0.50       ✓
   4.  아이티엠반도체(084850)       0.40/0.80    | 0.40/0.80       ✓
   5.  에스비비테크(389500)         0.40/0.70    | 0.40/0.70       ✓
   6.  유바이오로직스(206650)       0.40/0.70    | 0.40/0.70       ✓
   7.  해성에어로보틱스(059270)     0.40/0.70    | 0.40/0.70       ✓
   8.  아이센스(099190)             0.40/0.40    | 0.40/0.40       ✓
   9.  아이씨티케이(456010)         0.40/0.40    | 0.40/0.40       ✓
  10.  아이티켐(309710)             0.40/0.40    | 0.40/0.40       ✓

일치율: 10/10 = 100%
점수 정확도: 모든 max/total 소수점 2자리까지 일치
```

> **JS 매칭 엔진은 Python 매칭 모듈과 동일한 결과를 산출**한다. Layer A 음운 매칭, Layer B 의미 매칭, 점수 체계(0.30/0.40/0.50/0.55), 1자 토큰 페널티(×0.7), stopword 필터, 정렬 알고리즘 모두 동기화됨.

### 1.2 항목 4 상세 — KRX DB 일관성
```
CSV (data/krx_stocks.csv): 2,878 rows
JSON (data/krx_stocks.json): 2,878 rows
(code, name, market, marcap_eok) 튜플 집합 비교: 완전 동일

핵심 종목 검증:
  185490 CSV=아이진       JSON=아이진       ✓
  071950 CSV=코아스        JSON=코아스        ✓
  261780 CSV=차백신연구소  JSON=차백신연구소  ✓
  007540 CSV=샘표         JSON=샘표         ✓
```

> **데이터 이중 보관(CSV + JSON)이지만 변환 스크립트가 일관성 보장**.

---

## 2. 누락된 검증 (사용자 확인 필요)

| # | 항목 | 검증 방법 | 상태 |
|---|------|---------|------|
| E1 | 실제 E2E 흐름 (Collector → Forward → Stock Match 자동 매칭) | 브라우저 실행 | ❓ **사용자 검증 대기** |
| E2 | localStorage payload 무결성 (페이지 navigation 후 데이터 유지) | 브라우저 DevTools | ❓ 사용자 검증 대기 |
| E3 | GitHub Pages에서 KRX DB(116KB) fetch 시간 | 브라우저 Network 탭 | ❓ 사용자 검증 대기 |
| E4 | matcher_engine.js의 fromUrl() fetch 폴백 체인 | 브라우저 실행 | ❓ 사용자 검증 대기 |

→ 사용자가 브라우저에서 다음 시나리오 한 번 실행 후 보고하면 자동 검증으로 흡수 가능.

### 사용자 검증 시나리오
1. https://jyc0289y-art.github.io/dantalink/docs/data_collector_gui.html 접속
2. 키워드에 "샘 알트먼" 추가 (Enter)
3. ▶ INITIATE SCAN
4. ▶ FORWARD TO MATCHER
5. Stock Match 자동 로드 → 종목 카드 표시 여부
6. 상단 "FROM COLLECTOR (N)" 시나리오 배지 활성 여부
7. 브라우저 DevTools → Network 탭에서 `krx_stocks.json` 로드 시간 확인

---

## 3. QC 항목 표준화 (CLAUDE.md §품질검사 항목 확장 권장)

본 QC를 통해 발견한 표준 항목 보강 필요:

### 코드 변경 시
- [ ] pytest 회귀 (Python)
- [ ] import 무오류
- [ ] CLI --help 정상
- [ ] DeprecationWarning 동작 (rename 시)

### JS 모듈 추가 시 (신규 추가 권장)
- [ ] 브라우저 console.error 없음
- [ ] **Python 동등 로직과 cross-check** (Node 실행으로 자동 가능)
- [ ] DantaLink namespace 등록 정상

### 데이터 파일 추가 시 (신규 추가 권장)
- [ ] 원본과 일관성 (CSV↔JSON, DB↔dump 등)
- [ ] 파일 크기 합리성 (gzip 후 transfer 크기)
- [ ] 한글 인코딩 정상 (UTF-8 vs UTF-8-SIG)

### Pages 배포 시
- [ ] root `/` 200 OK
- [ ] 모든 GUI URL 200 OK
- [ ] 모든 JSON 산출물 200 OK
- [ ] 모든 JS 모듈 200 OK + Content-Type 검증

---

## 4. 본 QC가 작업 직후 수행됐어야 하는 이유

| 시점 | 영향 |
|------|------|
| **작업 직후 수행** (정상) | 매칭 일치성 미달 시 즉시 수정 가능, push 전 발견 |
| **사후 수행** (실제) | 이미 push된 코드를 신뢰할 근거가 retroactively 확보됨. 다행히 모두 통과했지만, 만약 불일치였다면 commit 이력에 오류 코드 남음 |

→ 향후 모든 작업은 **push 전 QC 보고서 작성 + 통과 확인 후 push**.

---

## 5. 다음 단계 진입 승인 여부

- ✅ 자동 검증 4/4 통과
- ❓ E2E 사용자 검증 대기 (4건)
- → **사용자 검증 보고 후 다음 분기 진입 권장** (외국 인명 패턴 매칭, GUI Live 매칭, KRX DB IndexedDB 캐시 등)
